'use client';

import SubmitPage from '@/app/[locale]/imams/submit/page';

// Maintenance submit page reuses the same multi-step form
export default function MaintenanceSubmitPage() {
    return <SubmitPage />;
}
